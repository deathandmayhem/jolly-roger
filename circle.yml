machine:
  node:
    version: 8.12.0
dependencies:
  pre:
    - npm install -g node-pre-gyp
  override:
    - ./deps.sh
  cache_directories:
    - 'tests/down/node_modules'
test:
  override:
    - ./test.sh
